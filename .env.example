# This repo is intentionally DB-free. If you later connect a DB, use ENV vars.
# NEVER commit real credentials. Keep .env local only.

DB_HOST=your_host
DB_PORT=5432
DB_NAME=your_db
DB_USER=your_user
DB_PASSWORD=your_password

# Optional app settings
FLASK_PORT=8055
